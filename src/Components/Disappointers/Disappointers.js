import React from "react";
import Loader from "../../loader/Loader";


export default function Disappointers(){
	return(
		<div>
			<h2 style={{fontSize: "35px", textAlign: "center", color: "var(--base_red)"}}>
			Work in Progress</h2>
			<Loader/>
		</div>
		)
}